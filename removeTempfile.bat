@echo off
color 0a
title Removing temporary code
echo This window removes the temporary file that Bob created to execute the code
echo You don't have a virus that is doing anything with this command prompt.
timeout /t 1
del tempfile.py
exit